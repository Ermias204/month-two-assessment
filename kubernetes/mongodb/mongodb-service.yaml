apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: muchtodo
  labels:
    app: mongodb
    component: database
spec:
  selector:
    app: mongodb
    component: database
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
  type: ClusterIP
